name = "nfr-asset-chat"
main = "worker.js"
compatibility_date = "2024-09-23"

# To add your OpenAI API key as an encrypted secret, run:
#   wrangler secret put OPENAI_API_KEY
# Then paste the key when prompted. It will never appear in plain text.
